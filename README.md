Dammyoung — Business Website

Files
- index.html — Main site entry
- css/style.css — Styles
- js/main.js — Minimal interactivity

View locally
1. Open `index.html` in your browser (double-click or use your editor's Live Server).

Optional: Serve with a simple static server

```bash
# Python 3 (in project folder)
python -m http.server 8080
# then open http://localhost:8080
```

Notes
- The contact form uses a `mailto:` fallback. Replace the `action` in the form with your server endpoint to collect submissions.
- Replace placeholder project text and images with real work.
- I can add a CMS, Netlify/ Vercel deploy config, or contact back-end if you want.